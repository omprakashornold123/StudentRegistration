spring:
  application:
    name: student-registration
  mvc:
    view:
      prefix: "/views/"
      suffix: .jsp
  datasource:
    url: "jdbc:mysql://localhost:3306/student_db?createDatabaseIfNotExist=true"
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 9090
  servlet:
    context-path: /student_registrations